import { Workspace } from "@/components/workspace/Workspace";
import { ArrowUpDown } from "lucide-react";

export const Sort = () => {
  return <Workspace.Action icon={ArrowUpDown}>Sort</Workspace.Action>;
};
