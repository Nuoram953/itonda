pre-commit:
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --all
      stage_fixed: true

    lint:
      run: pnpm -r lint

    generate-api:
      run: pnpm --filter @itonda/web generate-api
      glob: "*.{ts}"
      stage_fixed: true

